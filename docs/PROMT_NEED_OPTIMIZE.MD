Bạn lên plan và triển khai cho tôi các task sau nhé:
1/bạn xem qua  ảnh tôi đính kèm ấy, thì ở agent gen promt để tạo ảnh đăng bài thì bạn cần có thêm 1 promt để gen ảnh tương tự ảnh style trên nhé, và phần scenes mở rộng thêm về công ty, thang máy, khách sạn này nọ nữa nhé,...
2/
Bạn cần tìm hiểu trên mạng các trick để làm video timeslap với AI, và cho tôi các công cụ tạo video ổn nhất, chân thật nhất hiện tại.
Giờ tôi muốn triển khai thêm 1 AI tạo script để làm video timeslape bằng AI, cụ thể sẽ như sau, Tôi sẽ gửi 1 ảnh  bếp hay phòng tắm hay gì đó, và kèm theo ảnh 1 loại đá, sau đó AI sẽ nhận diện bếp đó là chỗ nào cần lát đá, chỗ nào cần giữ nguyên cảnh, và độ sáng và xác định hình ảnh loại đá tôi gửi, màu sắc, blabla,... sau đó AI sẽ cho tôi 1 script gồm các promt để gen ảnh cho từng công đoạn dựa vào 2 ảnh đấy (nghĩa là các ảnh này phải lấy bối cảnh là ảnh gốc tôi gửi và loại đá thì sẽ là loại đá tôi gửi (màu sắc, ánh sáng,...), mỗi công đoạn bạn cần có 1-2 đoạn ngắt mốc thời gian (nghĩa là nếu 6 công đoạn thì sẽ tầm 10-12 ảnh từng công đoạn đấy ấy), công đoạn hiện tại là ở file này:
@beautifulMention
và sau đó, AI sẽ cho tôi 1 các promt để nối 2 ảnh với nhau thành 1 video timeslap tầm 5-10s (nghĩa là với 10-12 ảnh kia thì sẽ cần 9-11 promt, do nối ảnh 1 với ảnh 2 thành 1 video, tiếp đó ảnh 2 - ảnh 3 thành 1 video, cho đến khi đến ảnh cuối cùng), mục đích là để thể hiện được sự mượt mà trong chuyển cảnh ấy, và không gây gián đoạn, do AI video chỉ có tạo video 5-10s thôi, nên giai đoạn này bạn cần cho tôi promt của từng cặp để tạo ra các video timeslap mô tả thực hiện từng công đoạn,
và cuối cùng bạn cho tôi caption để đăng video này kèm các hashtash cần thiết để video đc viral trên fb, insta
Kèm theo agent đó bạn cần tạo thêm 1 agent song song nữa, agent này thì rule sẽ thoải mái hơn không phải là tạo mẫu đá nữa mà là sẽ flexible theo ý tương của tôi, ví dụ tôi chỉ cần gửi ảnh 1 bãi đất trống và bảo là : triển khai cho tôi xây dựng sân bóng đá lớn nhất VN ở đây, thì AI sẽ lập tức nhận diện được và tạo script như agent kia cho tôi, là vẫn tạo ra nhiều công đoạn, đưa ra các promt tạo ảnh, sau đó đưa ra các promt tạo video timeslap tương ứng. (agent này sẽ flexible theo ý tương của tôi ấy)

3/ Hiện tại ở website đã ổn định rồi, nhưng có 1 số chỗ vẫn chưa có data ổn định,
3.1./ ở các trang detail của range như https://pvstone.com.au/catalog/artscut-zero/luxury-plus thì ảnh main ở trên cùng (ngay header) và ảnh phía dưới vẫn đang lấy chung là ảnh của item, làm nó bị lặp đi rất xâu và không đẹp, và tương tự với các range còn lại, bạn cần check qua db, xem các range nào đang lặp ảnh content như kia, thì bạn cần check trên mạng và đối ứng với range đó để tìm 1 ảnh phù hợp nhất để làm ảnh main và sub nhé (khôngđược trùng ảnh item) (nhớ cập nhật link ảnh mới đó vào cả db) và đảm bảo rà soát hết các range không được sót cái nào
3.2./ Phần project ddataa hiện tại cũng đang là data fake, nhìn vào rất giả, bạn cũng cần triển khai seed data thật vào nhé, và cũng tương ứng với mẫu đá đề cập ở project đó (mẫu đá này phải có ở tong product), và bài viết trong project này phải chân thật và đảm bảo yếu tố ổn định, đủ dài, và hình ảnh phải phản ánh được đá đang liệt kê
3.3/ ở trang home thì phần Stone Collection Gallery (Calacatta Lacebound - 01 of 05) đang thể hiện mẫu đá và ảnh phía trong chưa map với nhau ở 1 số thằng (cả tên nữa), bạn cần check lại image và đưa mẫu đá (có trong product) và ảnh nhà phù hợp nhé, và  thêm 1 nút nhấn để nhảy vào đúng product đá đó nữa nhé. (chữ view colour do nó nằm dưới quá lúc lướt khó thấy)

Bạn lên plan triển khai giúp tôi tất cả tasks trên nhé, nhớ đảm bảo thực hiện đầy đủ giúp tôi, verify lại kỹ càng không được sót
